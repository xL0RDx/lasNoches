let express,connectDatabase;_772‍.w("express",[["default",["express"],function(v){express=v}]]);_772‍.w("./config/db",[["default",["connectDatabase"],function(v){connectDatabase=v}]]);


// Initialize express application 
const app = express();

connectDatabase();

// API endpoints
app.get('/', (req,res) =>
    res.send('http got request sent to root api endpoint')
);

// Connection listener
app.listen(3000, () => console.log('Express server running on port 3000'));